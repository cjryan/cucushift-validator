<!--<h1>Pages#home</h1>
<p>Find me in app/views/pages/home.html.erb</p>-->

<script>
$(function() {
	$("#user_code").linedtextarea();
	/*$("#zebra_table").linedtextarea();*/
	$(".stripeMe tr:even").addClass("alt");

	$(".stripeMe tr").mouseover(function() {
		$(this).addClass("over");
	});

	$(".stripeMe tr").mouseout(function() {
		$(this).removeClass("over");
	});

});
</script>

<h1><%= @title %></h1>
<p>Your code: <%= @val_code %> </p>

<%= form_tag :action => 'home' do %>

   <%= text_area_tag 'user_code',  @val_code %>

   <%= submit_tag 'Validate' %>

<% end %>

<div id="zebra_table">
	<table class="stripeMe">
	<% @result.each do |iter| %>
	<!--If the variable contains 'true,' text color -> green. else, red. -->
		<%if iter =~ /true/%>
			<%= puts '<tr><td style="color: green">' %> <%= iter %> <%= puts '</td></tr>' %>
		<% end %>
	<% end %>
	</table>
</div><!-- end zebra_table -->
